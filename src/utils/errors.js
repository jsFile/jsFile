/**
 * @description error objects
 */
export const invalidFileType = 'Invalid file type. It must be an instance of File or Blob';
export const invalidLoadFile = `Can't load the file`;
export const invalidReadFile = `Can't read the file`;
export const requiredTechnologies = `Doesn't have required technologies`;
export const invalidParser = `Doesn't have a parser`;
export const invalidReadArchive = `Can't read the archive`;
export const notFoundMethodCreateDocument = 'Method `createDocument` not found';